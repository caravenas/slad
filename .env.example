# SLAD delegates model calls to a local agent CLI (claude, codex, ...).
# These are normally set by `slad setup` or per-run flags; override here if needed.
SLAD_CLI_BINARY=
SLAD_CLI_ARGS=
SLAD_CLI_PROMPT_MODE=
CLI_MODEL=
SLAD_LOG_LEVEL=info
