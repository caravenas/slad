# {{name}} (enterprise)

Generated by `slad create agent {{name}} --template enterprise`.

```
{{name}}/
├── slad.config.ts · package.json · .env.example · README.md
├── agents/        { orchestrator/, specialists/ }
├── tools/         { registry.ts, definitions/, mcp/ }
├── stages/ · pipelines/ · prompts/ · policies/
├── memory/ · runtime/ · api/
└── evals/ · observability/ · tests/ · docs/
```

This is the full §8 layout for a production agent system. Start from
`runtime/main.ts`, register tools in `tools/registry.ts`, and add stages/pipelines
as the system grows. The opt-in harness is configured in `slad.config.ts`.

## Run

```bash
cp .env.example .env   # add your provider key
pnpm install
pnpm start
```
